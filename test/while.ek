# This is a comment
extern int getarg(int);
extern float getargf(int);



def int run () {
    int $a = 3;
    while($a > 1) {
        $a = $a - 1;
    }


    while($a < 5) {
        $a = $a + 1;
    }

    return $a;
}

